# Outlook check — off for now

Grok 15-minute scheduler for `MTTech_SARFarm@outlook.com` was **cancelled** 2026-08-23 (Brian: turn off monitoring). Scheduler id `01a02fc36095`.

Local mail desk (`http://127.0.0.1:8780/`) may still be running on the Dell if the desktop shortcut is used. `may_send` stays false. Does not send. Does not touch 8767–8770.

GitHub Pages Actions workflow was emailing Hotmail on every push (“Get Pages site failed”). Workflow file deleted (commit `53dd81d`). 22 failure mails moved to Deleted Items 2026-08-23. **Do not** re-add `.github/workflows/pages.yml`. Enable Pages by hand in repo Settings if you want github.io.

To turn the 15-minute check back on, say so in the brainstorm TUI.
